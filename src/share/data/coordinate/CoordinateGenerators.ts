// Anonymous functions for abbreviating creation of different coordinate types.

// import { AbsoluteCoordinate } from ".";

// Lambda for creating absolute coordinates
// export const a = (x: number, y: number, z: number) => new AbsoluteCoordinate(x, y, z);
