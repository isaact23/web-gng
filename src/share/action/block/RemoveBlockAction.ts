import { IAbsoluteCoordinate } from "@share/data/coordinate";
import { Action } from "../Action";

export class RemoveBlockAction extends Action {
  constructor(
    coord: IAbsoluteCoordinate
  ) {
    super();
  }
}