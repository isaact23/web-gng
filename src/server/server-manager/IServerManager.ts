import { IGameServer } from "@server/server-manager/game-server";

/**
 * Handler for basic services connecting the server to the client,
 * i.e. express, socket.io, http, etc.
 */
export interface IServerManager {

}
