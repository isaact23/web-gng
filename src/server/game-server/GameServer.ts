import { IGameServer } from ".";

/**
 * Handler for server-side game logic.
 */
export class GameServer implements IGameServer {
  /**
   * Initialize this game server.
   */
  init(): void {
    
  }
}
